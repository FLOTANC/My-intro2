# Wake Path Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** デッサン専用トレーニングアプリ「Wake Path」を新規構築する。AI先生(Wake先生)が課題を出し9軸でデッサンを採点・対話し、先生が手描き添削を返し、生徒の上達を経過グラフ＋継続の仕掛けで可視化する。

**Architecture:** 独立Vercelアプリ `wake-path/`。単一HTML＋`/api/db.js`(firebase-admin・全書き込み集約・PIN照合)＋`/api/ai.js`(Gemini Vision：課題生成/採点/対話)。Firebaseは既存 `rowke-app` を流用しルートキー `dessin/` で分離。読み取りはクライアント直接(`.read:true`)、書き込みはAPIのみ(`.write:false`)。最初からセキュア。

**Tech Stack:** Vercel Serverless Functions(Node), firebase-admin, Firebase RTDB, Google Gemini 2.5 Flash (Vision), Vanilla JS(単一HTML・CDN), 素のCanvas/SVG。

**検証方針:** テストランナーなし。`node --check`＋Vercelプレビュー/本番＋curl＋ブラウザ(プレビューサーバー)での手動検証。仕様の各受け入れ基準を対応タスクで確認。

---

## 前提（ユーザー作業・実装前に確認）

`rowke-app` Firebase の `dessin/` 配下を使う。**新Firebaseプロジェクトは不要**。ただしルールに `dessin` を追加する必要がある（ユーザー作業）：

1. Firebase Console → `rowke-app` → Realtime Database → ルールに `dessin` ブロックを追加して公開：
   ```json
   {
     "rules": {
       "art":    { ".read": true, ".write": false },
       "dessin": { ".read": true, ".write": false }
     }
   }
   ```
2. 新Vercelプロジェクト `wake-path` 作成時に、環境変数を rowke と同じ値で設定（Production/Preview）：
   `FIREBASE_SERVICE_ACCOUNT` / `FIREBASE_DB_URL` / `GEMINI_API_KEY` / `TEACHER_PIN`
   （Task 1 のデプロイ時に `vercel` CLI が新プロジェクトを作るので、その後ダッシュボードで環境変数を設定）

> コントローラーは Task 1 後に「環境変数4つを wake-path プロジェクトに設定して」とユーザーに案内し、設定完了を待ってから Task 2 の検証に進む。

## ディレクトリ / ファイル構成

```
wake-path/
  index.html        … 単一HTML（CSS/JS インライン）。4画面＋先生モード
  api/db.js         … 書き込みAPI（dessin用アクション・PIN照合・firebase-admin）
  api/ai.js         … Gemini Vision（assignmentDraft / assignmentChat / review / chat）
  package.json      … firebase-admin 依存
  .gitignore        … node_modules / .vercel / .env*
```

## データ契約（全タスク共通・`dessin/` 配下）

```
dessin/students/{studentId}
  name, grade, targetSchool, examDate, examInfo, level, createdAt
  streak: { count, lastSubmitDate }     // lastSubmitDate は 'YYYY-M-D'
  badges: { [badgeKey]: ts }
  bestScores: { proportion,volume,texture,composition,observation,space,light,structure,process }

dessin/assignments/{assignmentId}
  studentName, motif, focus, stage('基礎'|'応用'|'実戦'), timeLimit(分,必須),
  origin('ai'|'teacher'), aiDraft, teacherChat:[{role,text,ts}], status('draft'|'published'), ts

dessin/submissions/{submissionId}
  studentName, assignmentId, assignment:{motif,focus,stage,timeLimit},
  imageBase64, ts,
  aiReview: { scores:{<9軸>}, comment, passGap, nextAssignment },
  aiChat: [{q,a,ts}],
  teacherReview: { annotatedImage, comment, ts }
```

9軸キー（順序固定）: `proportion, volume, texture, composition, observation, space, light, structure, process`

---

### Task 1: プロジェクト雛形＋初回デプロイ

**Files:** Create `wake-path/package.json`, `wake-path/.gitignore`, `wake-path/index.html`(最小シェル)

- [ ] **Step 1: package.json**

`wake-path/package.json`:
```json
{
  "dependencies": {
    "firebase-admin": "^13.0.0"
  }
}
```

- [ ] **Step 2: .gitignore**

`wake-path/.gitignore`:
```
node_modules
.vercel
.env
.env*.local
```

- [ ] **Step 3: 最小 index.html**

`wake-path/index.html`:
```html
<!DOCTYPE html>
<html lang="ja">
<head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Wake Path</title></head>
<body><h1>Wake Path</h1><p>準備中</p></body>
</html>
```

- [ ] **Step 4: 依存インストール＆デプロイ**

```bash
cd "/Users/fujitatakako/デスクトップ/claude/wake-path"
npm install
vercel --yes   # 新プロジェクト wake-path を作成（対話があればプロジェクト名 wake-path で作成）
```
Expected: プレビューURLが ready。

- [ ] **Step 5: コミット**
```bash
cd "/Users/fujitatakako/デスクトップ/claude"
git add wake-path/package.json wake-path/.gitignore wake-path/index.html wake-path/package-lock.json
git commit -m "feat: scaffold Wake Path app"
```

- [ ] **Step 6（ユーザー作業の案内）:** コントローラーはユーザーに「wake-path プロジェクトに環境変数4つ(FIREBASE_SERVICE_ACCOUNT/FIREBASE_DB_URL/GEMINI_API_KEY/TEACHER_PIN)を設定」＋「rowke-app のルールに `dessin` 行を追加」を案内し、完了を待つ。

---

### Task 2: `api/db.js`（書き込みAPI）

**Files:** Create `wake-path/api/db.js`

- [ ] **Step 1: db.js を作成**

```js
// Wake Path 書き込みAPI。dessin/ 配下のみ。先生系アクションは x-teacher-pin 照合。
const admin = require('firebase-admin');
function getDb() {
  if (!admin.apps.length) {
    admin.initializeApp({
      credential: admin.credential.cert(JSON.parse(process.env.FIREBASE_SERVICE_ACCOUNT)),
      databaseURL: process.env.FIREBASE_DB_URL
    });
  }
  return admin.database();
}
const S = s => String(s || '').replace(/[.#$\[\]/]/g, '_');
const now = () => Date.now();
const AXES = ['proportion','volume','texture','composition','observation','space','light','structure','process'];

const TEACHER_ACTIONS = new Set([
  'saveStudent','deleteStudent',
  'assignmentDraftSave','assignmentChatAppend','assignmentPublish','assignmentDelete',
  'teacherReview'
]);
function pinOk(req){ const p=req.headers['x-teacher-pin']; return !!p && !!process.env.TEACHER_PIN && p===process.env.TEACHER_PIN; }
const todayStr = () => { const d=new Date(); return `${d.getFullYear()}-${d.getMonth()+1}-${d.getDate()}`; };

module.exports = async (req, res) => {
  res.setHeader('Access-Control-Allow-Origin','*');
  res.setHeader('Access-Control-Allow-Methods','POST, OPTIONS');
  res.setHeader('Access-Control-Allow-Headers','Content-Type, x-teacher-pin');
  if (req.method==='OPTIONS') return res.status(200).end();
  if (req.method!=='POST') return res.status(405).json({error:'Method not allowed'});

  const { action, payload={} } = req.body || {};
  if (!action) return res.status(400).json({error:'action is required'});

  if (action==='verifyPin') {
    if (payload.pin && process.env.TEACHER_PIN && payload.pin===process.env.TEACHER_PIN) return res.status(200).json({ok:true});
    return res.status(403).json({error:'PINが違います'});
  }
  const teacher = pinOk(req);
  if (TEACHER_ACTIONS.has(action) && !teacher) return res.status(403).json({error:'この操作は先生のみ行えます'});

  try {
    const db = getDb();
    switch (action) {

      // ---- 先生：生徒管理 ----
      case 'saveStudent': {
        const p = payload;
        if (!p.name) return res.status(400).json({error:'name required'});
        const id = p.studentId || db.ref('dessin/students').push().key;
        const cur = (await db.ref('dessin/students/'+S(id)).once('value')).val() || {};
        const rec = {
          name: String(p.name), grade: String(p.grade||''),
          targetSchool: String(p.targetSchool||''), examDate: String(p.examDate||''),
          examInfo: String(p.examInfo||''), level: String(p.level||'初級'),
          createdAt: cur.createdAt || now(),
          streak: cur.streak || { count:0, lastSubmitDate:'' },
          badges: cur.badges || {}, bestScores: cur.bestScores || {}
        };
        await db.ref('dessin/students/'+S(id)).set(rec);
        return res.status(200).json({ok:true, studentId:id});
      }
      case 'deleteStudent': {
        await db.ref('dessin/students/'+S(payload.studentId)).remove();
        return res.status(200).json({ok:true});
      }

      // ---- 先生：課題づくり ----
      case 'assignmentDraftSave': {
        const p = payload;
        const id = p.assignmentId || db.ref('dessin/assignments').push().key;
        const cur = (await db.ref('dessin/assignments/'+S(id)).once('value')).val() || {};
        const rec = {
          studentName: String(p.studentName||cur.studentName||''),
          motif: String(p.motif||''), focus: String(p.focus||''),
          stage: String(p.stage||'基礎'), timeLimit: parseInt(p.timeLimit,10)||60,
          origin: p.origin || cur.origin || 'teacher',
          aiDraft: p.aiDraft || cur.aiDraft || null,
          teacherChat: cur.teacherChat || [],
          status: 'draft', ts: now()
        };
        await db.ref('dessin/assignments/'+S(id)).set(rec);
        return res.status(200).json({ok:true, assignmentId:id});
      }
      case 'assignmentChatAppend': {
        const ref = db.ref('dessin/assignments/'+S(payload.assignmentId)+'/teacherChat');
        await ref.push({ role:String(payload.role||'teacher'), text:String(payload.text||''), ts:now() });
        return res.status(200).json({ok:true});
      }
      case 'assignmentPublish': {
        await db.ref('dessin/assignments/'+S(payload.assignmentId)).update({ status:'published', ts:now() });
        return res.status(200).json({ok:true});
      }
      case 'assignmentDelete': {
        await db.ref('dessin/assignments/'+S(payload.assignmentId)).remove();
        return res.status(200).json({ok:true});
      }

      // ---- 生徒：提出（誰でも）----
      case 'submit': {
        const p = payload;
        if (!p.studentName || !/^data:image\//.test(p.imageBase64||'')) return res.status(400).json({error:'studentName/image required'});
        const rec = {
          studentName:String(p.studentName), assignmentId:String(p.assignmentId||''),
          assignment: p.assignment || {}, imageBase64:p.imageBase64, ts:now()
        };
        const ref = await db.ref('dessin/submissions').push(rec);
        // ストリーク更新
        const sId = p.studentId;
        if (sId) {
          const sref = db.ref('dessin/students/'+S(sId)+'/streak');
          const st = (await sref.once('value')).val() || { count:0, lastSubmitDate:'' };
          const today = todayStr();
          if (st.lastSubmitDate !== today) {
            const y = new Date(); y.setDate(y.getDate()-1);
            const yStr = `${y.getFullYear()}-${y.getMonth()+1}-${y.getDate()}`;
            st.count = (st.lastSubmitDate===yStr) ? (st.count||0)+1 : 1;
            st.lastSubmitDate = today;
            await sref.set(st);
          }
        }
        return res.status(200).json({ok:true, submissionId:ref.key});
      }
      case 'saveReview': { // AI採点結果の保存（誰でも＝/api/ai 経由で生成した値を保存）
        const p = payload;
        if (!p.submissionId || !p.aiReview) return res.status(400).json({error:'submissionId/aiReview required'});
        await db.ref('dessin/submissions/'+S(p.submissionId)+'/aiReview').set(p.aiReview);
        // 自己ベスト更新＋更新軸を返す
        const updated = [];
        if (p.studentId && p.aiReview.scores) {
          const bref = db.ref('dessin/students/'+S(p.studentId)+'/bestScores');
          const best = (await bref.once('value')).val() || {};
          for (const k of AXES) {
            const v = p.aiReview.scores[k];
            if (typeof v==='number' && (!(k in best) || v>best[k])) { best[k]=v; updated.push(k); }
          }
          await bref.set(best);
        }
        return res.status(200).json({ok:true, bestUpdated:updated});
      }
      case 'aiChatAppend': { // 生徒とWake先生の対話ログ（誰でも）
        await db.ref('dessin/submissions/'+S(payload.submissionId)+'/aiChat').push({
          q:String(payload.q||''), a:String(payload.a||''), ts:now()
        });
        return res.status(200).json({ok:true});
      }
      case 'awardBadge': { // バッジ付与（誰でも。べき等）
        await db.ref('dessin/students/'+S(payload.studentId)+'/badges/'+S(payload.badgeKey)).set(now());
        return res.status(200).json({ok:true});
      }

      // ---- 先生：手描き添削 ----
      case 'teacherReview': {
        const p = payload;
        if (!/^data:image\//.test(p.annotatedImage||'')) return res.status(400).json({error:'invalid image'});
        await db.ref('dessin/submissions/'+S(p.submissionId)+'/teacherReview').set({
          annotatedImage:p.annotatedImage, comment:String(p.comment||''), ts:now()
        });
        return res.status(200).json({ok:true});
      }

      default: return res.status(400).json({error:'unknown action: '+action});
    }
  } catch (err) {
    console.error('db api error:', action, err);
    return res.status(500).json({error: err.message || 'Internal server error'});
  }
};
```

- [ ] **Step 2: 構文チェック** `node --check wake-path/api/db.js` → エラーなし
- [ ] **Step 3: デプロイ** `cd wake-path && vercel --prod --yes`（環境変数設定済み前提）
- [ ] **Step 4: 検証**
```bash
# 誤PIN → 403
curl -s -X POST <PROD_URL>/api/db -H 'Content-Type: application/json' -d '{"action":"verifyPin","payload":{"pin":"0000"}}'
# PINなし先生アクション → 403
curl -s -X POST <PROD_URL>/api/db -H 'Content-Type: application/json' -d '{"action":"saveStudent","payload":{"name":"x"}}'
# 不明アクション → 400
curl -s -X POST <PROD_URL>/api/db -H 'Content-Type: application/json' -d '{"action":"nope"}'
```
Expected: `PINが違います` / `この操作は先生のみ行えます` / `unknown action: nope`
- [ ] **Step 5: コミット** `git commit -m "feat: Wake Path write API with PIN"`

---

### Task 3: `api/ai.js`（Gemini Vision）

**Files:** Create `wake-path/api/ai.js`

- [ ] **Step 1: ai.js を作成**

```js
// Wake Path AI（Gemini Vision）。課題ドラフト生成・先生との課題相談・採点・生徒対話。
const MODELS = ['gemini-2.5-flash','gemini-2.5-flash-lite'];

const AXES = [
  ['proportion','形・プロポーション（比率・角度・傾き・左右差）'],
  ['volume','量感・立体（面・明暗・丸み・厚み）'],
  ['texture','質感・固有色の描き分け（材質の違い／固有色を無彩色の明度に置換し三段階程度に整理できているか）'],
  ['composition','構図・画面構成（配置・余白・見せ場・密度配分）'],
  ['observation','観察・描き込み（情報量・細部・粘り・エッジの描き分け）'],
  ['space','空間把握（奥行き・接地・前後関係・パース）'],
  ['light','光源理解（光・影・反射光・投影の一貫性・階調の幅）'],
  ['structure','構造理解（見えない内部形態・軸・骨格）'],
  ['process','制作プロセス（大づかみ・修正力・全体管理）']
];

const PERSONA = `あなたはデッサン専用トレーニングアプリ「Wake Path」のAI講師「Wake先生」です。
長年デッサンを見てきた、優しく少し茶目っ気のある先生。生徒一人ひとりを尊重し、宝物のように扱う。
中高生の美術系受験生を、基礎から志望校合格レベルまで導くのが役目。
口調は時々「〜じゃのう」等の柔らかい言い回しを自然に少しだけ。生徒は名前で呼ぶ。「お前」等は使わない。
安全ルール：深刻な悩み（いじめ・心身のつらさ・家庭）は気持ちを受け止めたうえで必ず「先生やおうちの人に直接話してほしいのう」と人間へ橋渡しし、詳細は聞き出さない。住所・学校名・電話等の個人情報は聞かない／書かれても繰り返さない。`;

function safety(){ return [
  {category:'HARM_CATEGORY_HARASSMENT',threshold:'BLOCK_MEDIUM_AND_ABOVE'},
  {category:'HARM_CATEGORY_HATE_SPEECH',threshold:'BLOCK_MEDIUM_AND_ABOVE'},
  {category:'HARM_CATEGORY_SEXUALLY_EXPLICIT',threshold:'BLOCK_LOW_AND_ABOVE'},
  {category:'HARM_CATEGORY_DANGEROUS_CONTENT',threshold:'BLOCK_MEDIUM_AND_ABOVE'}
];}

async function gen(body){
  let lastErr='';
  for (const model of MODELS){
    for (let i=0;i<2;i++){
      const r = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${model}:generateContent?key=${process.env.GEMINI_API_KEY}`,
        { method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify(body) });
      if (r.ok){ const d=await r.json(); const t=d.candidates?.[0]?.content?.parts?.[0]?.text; if(t) return t; lastErr='empty'; continue; }
      const e=await r.json().catch(()=>({})); lastErr=e.error?.message||('Gemini '+r.status);
      if (![429,500,503].includes(r.status)) break;
      await new Promise(s=>setTimeout(s,600));
    }
  }
  throw new Error(lastErr||'generation failed');
}
function dataUrlToPart(dataUrl){
  const m = /^data:(image\/[a-zA-Z+]+);base64,(.+)$/.exec(dataUrl||'');
  if (!m) return null;
  return { inline_data: { mime_type:m[1], data:m[2] } };
}
function studentCtx(s){
  if (!s) return '';
  return `生徒情報：名前=${s.name||''}／学年=${s.grade||''}／志望校=${s.targetSchool||''}／受験時期=${s.examDate||''}／レベル=${s.level||''}\n志望校の出題傾向・合格基準（最優先の判断基準）：${s.examInfo||'（未入力）'}`;
}
function axesList(){ return AXES.map(([k,d])=>`- ${k}: ${d}`).join('\n'); }

module.exports = async (req, res) => {
  res.setHeader('Access-Control-Allow-Origin','*');
  res.setHeader('Access-Control-Allow-Methods','POST, OPTIONS');
  res.setHeader('Access-Control-Allow-Headers','Content-Type');
  if (req.method==='OPTIONS') return res.status(200).end();
  if (req.method!=='POST') return res.status(405).json({error:'Method not allowed'});
  const { action, payload={} } = req.body || {};

  try {
    if (action==='assignmentDraft'){
      const sys = PERSONA + `\n\n次のデッサン課題を1つ提案する。カリキュラム段階(基礎→応用→実戦)を踏まえ、直近の弱点軸を伸ばす課題にする。
出力はJSONのみ：{"motif":"…","focus":"…","stage":"基礎|応用|実戦","timeLimit":数値(分),"reason":"なぜこの課題か(先生向け1-2文)"}`;
      const user = `${studentCtx(payload.student)}\n直近スコア(9軸,低いほど弱点)：${JSON.stringify(payload.recentScores||{})}\n受験までの目安：${payload.student?.examDate||'不明'}\nJSONだけ返す。`;
      const text = await gen({ system_instruction:{parts:[{text:sys}]}, contents:[{role:'user',parts:[{text:user}]}],
        generationConfig:{temperature:0.7,maxOutputTokens:600,thinkingConfig:{thinkingBudget:0},responseMimeType:'application/json'}, safetySettings:safety() });
      return res.status(200).json({ draft: JSON.parse(text) });
    }

    if (action==='assignmentChat'){ // 先生がAIと課題を相談
      const sys = PERSONA + `\nあなたは先生と一緒にデッサン課題を練っている。先生の要望（例：もっと質感重視、制限時間90分）を反映し、更新後の課題JSONと一言コメントを返す。
出力JSONのみ：{"motif":"…","focus":"…","stage":"基礎|応用|実戦","timeLimit":数値,"message":"先生への一言"}`;
      const conv = (payload.teacherChat||[]).map(m=>`${m.role==='teacher'?'先生':'Wake先生'}：${m.text}`).join('\n');
      const user = `${studentCtx(payload.student)}\n現在の課題案：${JSON.stringify(payload.current||{})}\nこれまでの相談：\n${conv}\n先生の新しい要望：${payload.message||''}\nJSONだけ返す。`;
      const text = await gen({ system_instruction:{parts:[{text:sys}]}, contents:[{role:'user',parts:[{text:user}]}],
        generationConfig:{temperature:0.7,maxOutputTokens:700,thinkingConfig:{thinkingBudget:0},responseMimeType:'application/json'}, safetySettings:safety() });
      return res.status(200).json({ result: JSON.parse(text) });
    }

    if (action==='review'){ // 提出画像を9軸採点
      const part = dataUrlToPart(payload.imageBase64);
      if (!part) return res.status(400).json({error:'image required'});
      const sys = PERSONA + `\n提出されたデッサンを、受験基準で次の9軸で1〜5点採点する（5が最高）。各軸の意味：\n${axesList()}\n
講評は具体的でやさしく。合格ギャップ(passGap)は志望校合格ラインまであと何が足りないかを1〜2点に絞って具体的に。nextAssignmentは次に取り組むと良い課題を一言で。
出力はJSONのみ：{"scores":{"proportion":n,"volume":n,"texture":n,"composition":n,"observation":n,"space":n,"light":n,"structure":n,"process":n},"comment":"…","passGap":"…","nextAssignment":"…"}`;
      const user = `${studentCtx(payload.student)}\n今回の課題：${JSON.stringify(payload.assignment||{})}\nこの画像を採点してJSONだけ返す。`;
      const text = await gen({ system_instruction:{parts:[{text:sys}]}, contents:[{role:'user',parts:[{text:user},part]}],
        generationConfig:{temperature:0.6,maxOutputTokens:1200,thinkingConfig:{thinkingBudget:0},responseMimeType:'application/json'}, safetySettings:safety() });
      return res.status(200).json({ review: JSON.parse(text) });
    }

    if (action==='chat'){ // その提出について生徒と対話
      const sys = PERSONA + `\n生徒のデッサンと、あなたの採点・先生の添削コメントを踏まえ、生徒の質問に短く(3〜5文)やさしく答える。考えを引き出す問いも織り交ぜる。`;
      const ctx = `あなたの採点：${JSON.stringify(payload.aiReview||{})}\n先生の添削コメント：${payload.teacherComment||'（なし）'}`;
      const parts = [{text:`${studentCtx(payload.student)}\n${ctx}\n生徒の質問：${payload.question||''}`}];
      const part = dataUrlToPart(payload.imageBase64); if (part) parts.push(part);
      const text = await gen({ system_instruction:{parts:[{text:sys}]}, contents:[{role:'user',parts}],
        generationConfig:{temperature:0.85,maxOutputTokens:800,thinkingConfig:{thinkingBudget:0}}, safetySettings:safety() });
      return res.status(200).json({ answer: text });
    }

    return res.status(400).json({error:'unknown action: '+action});
  } catch (err) {
    console.error('ai api error:', action, err);
    return res.status(500).json({error: err.message || 'Internal server error'});
  }
};
```

- [ ] **Step 2: 構文チェック** `node --check wake-path/api/ai.js`
- [ ] **Step 3: デプロイ** `cd wake-path && vercel --prod --yes`
- [ ] **Step 4: 検証（assignmentDraft はPIN不要・画像不要なので叩ける）**
```bash
curl -s -X POST <PROD_URL>/api/ai -H 'Content-Type: application/json' \
 -d '{"action":"assignmentDraft","payload":{"student":{"name":"テスト","grade":"高2","targetSchool":"芸大","level":"中級","examInfo":"鉛筆デッサン3時間"},"recentScores":{"light":2,"space":2}}}'
```
Expected: `{"draft":{"motif":...,"focus":...,"stage":...,"timeLimit":...}}`（弱点 light/space に寄った課題）
- [ ] **Step 5: コミット** `git commit -m "feat: Wake Path AI endpoint (assignment/review/chat)"`

---

### Task 4: クライアント基盤（HTML骨格・Firebase読み取り・PIN・ラッパー・ナビ）

**Files:** Modify `wake-path/index.html`（最小シェルを本体に置換）

この Task で index.html を以下の構造にする（CSS/JSインライン）。**rowke の構成・命名規約に倣う**。

- [ ] **Step 1: 骨格・スタイル・Firebase初期化・ラッパー・ナビを実装**

`wake-path/index.html` 全体を作成。要点（完全コードで実装すること）：

1. `<head>`：Google Fonts（Poppins/Inter）、`:root` CSS変数（rowke海・青系：`--navy:#0e3a52` 等）、ベースCSS。
2. Firebase CDN（`firebase-app-compat.js` / `firebase-database-compat.js` 10.14.1）。
3. `firebaseConfig`（rowke-app の公開設定。index.html/rowke と同値）→ `firebase.initializeApp` → `const db = firebase.database();`。
4. 状態：`let students={}, assignments={}, submissions={};` を `db.ref('dessin/students'|'dessin/assignments'|'dessin/submissions').on('value', …)` で購読し、各 render を呼ぶ。
5. 身元：localStorage `wakepath_viewer`（{role:'student'|'teacher', name}）と `wakepath_teacher_pin`。`getViewer()/setViewer()/isTeacher()`。
6. 通信ラッパー：
```js
function teacherPin(){ try{return localStorage.getItem('wakepath_teacher_pin')||'';}catch(e){return '';} }
async function dbWrite(action,payload){
  const h={'Content-Type':'application/json'}; const p=teacherPin(); if(p) h['x-teacher-pin']=p;
  const r=await fetch('/api/db',{method:'POST',headers:h,body:JSON.stringify({action,payload})});
  const d=await r.json().catch(()=>({})); if(!r.ok||d.error) throw new Error(d.error||('HTTP '+r.status)); return d;
}
async function aiCall(action,payload){
  const r=await fetch('/api/ai',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,payload})});
  const d=await r.json().catch(()=>({})); if(!r.ok||d.error) throw new Error(d.error||('HTTP '+r.status)); return d;
}
function compressImage(file,max,cb){ const img=new Image(); const rd=new FileReader();
  rd.onload=e=>{img.onload=()=>{const s=Math.min(1,max/Math.max(img.width,img.height));const c=document.createElement('canvas');
  c.width=Math.round(img.width*s);c.height=Math.round(img.height*s);c.getContext('2d').drawImage(img,0,0,c.width,c.height);
  cb(c.toDataURL('image/jpeg',0.85));};img.src=e.target.result;};rd.readAsDataURL(file); }
```
7. 画面コンテナ `.screen`（`#screen-today`/`#screen-submit`/`#screen-history`/`#screen-teacher`）＋下部ナビ＋`switchTab(name)`（rowke と同パターン：active 切替、render 呼び出し）。
8. 生徒選択モーダル（初回 `getViewer()` が無ければ表示）：生徒一覧から選ぶ／「先生として入る」→PIN入力→`verifyPin`→成功で `wakepath_teacher_pin` 保存。rowke の showViewerModal/checkTeacherPw を踏襲。

- [ ] **Step 2: 構文チェック**
```bash
cd "/Users/fujitatakako/デスクトップ/claude/wake-path"
node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const b=[...h.matchAll(/<script>([\s\S]*?)<\/script>/g)];let ok=1;b.forEach((m,i)=>{try{new Function(m[1])}catch(e){console.log(i,e.message);ok=0}});console.log(ok?'OK':'ERR')"
```
- [ ] **Step 3: プレビューサーバーで表示確認**（`.claude/launch.json` の静的サーバー or ルートの flotan サーバーで `/wake-path/index.html`）。コンソールにエラーが無く、生徒選択モーダルが出ること。読み取りは本番Firebaseに繋がる（dessin はまだ空でもエラーにならない）。
- [ ] **Step 4: コミット** `git commit -m "feat: Wake Path client shell, firebase read, wrappers, nav"`

---

### Task 5: 先生モード（生徒管理＋課題づくり）

> 提出より先に「生徒と課題」が要るので先に作る。

**Files:** Modify `wake-path/index.html`（`#screen-teacher` と関連JS）

- [ ] **Step 1: 生徒管理UI＋保存**
  - 生徒一覧（students から）＋「生徒を追加」フォーム：name/grade/targetSchool/examDate/examInfo(textarea)/level(select 初級/中級/受験直前)
  - 保存 → `dbWrite('saveStudent', {...})`。編集も同フォーム（studentId 付き）。削除 → `dbWrite('deleteStudent',{studentId})`
- [ ] **Step 2: 課題づくりUI（AIと相談）**
  - 生徒を選ぶ → 「AIに課題を提案してもらう」→ `aiCall('assignmentDraft',{student, recentScores})`（recentScores はその生徒の直近 submission の aiReview.scores、無ければ {}）→ フォーム(motif/focus/stage/timeLimit) に反映＋`dbWrite('assignmentDraftSave',{studentName,...,origin:'ai',aiDraft})`
  - 相談欄：先生が要望を入力 → `dbWrite('assignmentChatAppend',{assignmentId,role:'teacher',text})` → `aiCall('assignmentChat',{student,current,teacherChat,message})` → 返り JSON でフォーム更新＋`assignmentChatAppend(role:'ai', text:message)` ＋ `assignmentDraftSave` で上書き
  - 「この課題を生徒に出す」→ `dbWrite('assignmentPublish',{assignmentId})`
  - 手動作成（AIなし）も可：フォーム直接入力→ draftSave→publish
  - timeLimit 未入力は保存不可（必須）。バリデーション alert。
- [ ] **Step 3: 検証（プレビュー＋本番）**：先生PINで入り、生徒を1人作成→課題をAI提案→相談で1回調整→公開。Firebaseに students/{} と assignments/{status:published} ができることを確認（REST or コンソール）。
- [ ] **Step 4: コミット** `git commit -m "feat: teacher mode (students + AI-collaborative assignments)"`

---

### Task 6: きょうの課題＋タイマー＋ログインボーナス/ストリーク

**Files:** Modify `wake-path/index.html`（`#screen-today`）

- [ ] **Step 1: 当日課題の表示**
  - ログイン中生徒の published 最新 assignment を表示（motif/focus/stage/制限時間）。無ければ「先生が課題を準備中じゃ」表示。
  - **タイマー**：制限時間からのカウントダウン（開始ボタン→分:秒表示→0で「時間です」通知）。`setInterval`、画面離脱で停止。
  - 「描いたら写真をアップ」→ `switchTab('submit')`。
- [ ] **Step 2: ログインボーナス／ストリーク表示**
  - 生徒の `streak.count` を「🔥 ○日連続」表示。
  - 日付が変わって初回表示時、`localStorage('wakepath_lastseen_'+name)` と今日を比較し、今日初回なら Wake先生からの一言（固定文＋日替わりのやさしい励まし数種からランダム）をカード表示。
- [ ] **Step 3: 検証**：生徒でログイン→公開済み課題とタイマー、ストリーク表示、初回アクセスで励ましが出る。
- [ ] **Step 4: コミット** `git commit -m "feat: today's assignment, timer, streak & login bonus"`

---

### Task 7: 提出＆AI講評（写真→9軸採点→対話＋自己ベスト演出）

**Files:** Modify `wake-path/index.html`（`#screen-submit`）

- [ ] **Step 1: 提出フロー**
  - 写真選択 → `compressImage(file,1400,b64=>…)` プレビュー → 「提出する」
  - `dbWrite('submit',{studentId,studentName,assignmentId,assignment,imageBase64})` → submissionId 取得
  - 続けて `aiCall('review',{student,assignment,imageBase64})` → review 取得 → `dbWrite('saveReview',{submissionId,studentId,aiReview:review})` → 返り `bestUpdated` を取得
- [ ] **Step 2: 採点表示**
  - 9軸を 1〜5 のバー/★で表示、`comment`／`passGap`（合格まであと何が足りないか）／`nextAssignment` を表示
  - `bestUpdated` が非空なら「自己ベスト！」演出（CSSアニメ＋簡単な紙吹雪 canvas、軽量）
  - マイルストーン判定：提出累計や「ある軸で初の5点」で `dbWrite('awardBadge',{studentId,badgeKey})`
- [ ] **Step 3: その提出で対話**
  - 入力 → `aiCall('chat',{student,aiReview,teacherComment,imageBase64,question})` → 表示 → `dbWrite('aiChatAppend',{submissionId,q,a})`
  - 送信はボタンのみ（Enter誤送信なし、rowke の教訓）
- [ ] **Step 4: 検証（本番）**：実際に1枚提出 → 9軸採点が返る → 講評/passGap表示 → 対話できる → bestScores が更新される（Firebase確認）。
- [ ] **Step 5: コミット** `git commit -m "feat: submission, AI 9-axis review, chat, best-score celebration"`

---

### Task 8: これまで（経過）＝時系列＋成長グラフ＋進捗バー＋バッジ

**Files:** Modify `wake-path/index.html`（`#screen-history`）

- [ ] **Step 1: 時系列カード**
  - ログイン中生徒の submissions を新しい順に。各カード：サムネ・課題名・日付・9軸ミニ表示・先生添削済みバッジ（teacherReview があれば「✔ 先生添削」表示、タップで添削画像＋コメント表示）
- [ ] **Step 2: 成長グラフ（9軸の推移）**
  - 素のSVGで折れ線（横=提出回, 縦=1〜5）。9軸を凡例つきで重ねる、または「総合点(9軸平均)の推移」＋「最新回のレーダー(9軸)」の2本立て（実装が軽い方を選ぶ。レーダーは多角形パスをSVGで描く）
- [ ] **Step 3: 合格ライン進捗バー**
  - 最新の9軸平均 ÷ 目標(例:合格ライン=各軸4.0→平均4.0) を % で。`(avgLatest/4.0)*100` をバー表示。「合格ラインまであと○」テキスト。
- [ ] **Step 4: 獲得バッジ表示**：students/{}.badges を一覧（アイコン＋名称）。
- [ ] **Step 5: 検証**：複数提出後、時系列・グラフ・進捗バー・バッジが正しく描画/更新される。
- [ ] **Step 6: コミット** `git commit -m "feat: history timeline, growth chart, pass-line bar, badges"`

---

### Task 9: 先生の手描き添削（Canvas）

**Files:** Modify `wake-path/index.html`（`#screen-teacher` 内「未添削一覧」＋添削Canvas）

- [ ] **Step 1: 未添削一覧**：submissions のうち teacherReview 未設定のものを生徒名つきで一覧。タップで添削画面へ。
- [ ] **Step 2: 添削Canvas**
  - 生徒画像を背景に描画した `<canvas>`（画像をdrawImageで敷く）。pointerdown/move/up で赤線フリーハンド。線幅選択、取り消し(直前ストロークpop→再描画)、全消し(画像だけ再描画)。
  - 「保存」→ `canvas.toDataURL('image/jpeg',0.85)` を `annotatedImage` として `dbWrite('teacherReview',{submissionId,annotatedImage,comment})`（PIN必須）
  - ストローク配列を保持して undo を実現（背景画像＋全ストローク再描画）。
- [ ] **Step 3: 検証**：先生PINで未添削の提出を開き、赤線を描いてコメント付き保存 → 生徒の「これまで」に「✔先生添削」と添削画像が出る。
- [ ] **Step 4: コミット** `git commit -m "feat: teacher freehand annotation (canvas) + comment"`

---

### Task 10: 仕上げ・本番E2E

- [ ] **Step 1: 本番デプロイ** `cd wake-path && vercel --prod --yes`
- [ ] **Step 2: 通しE2E（本番URL）**
  - 先生：生徒作成→課題をAIと相談して公開→（後で）未添削を手描き添削
  - 生徒：きょうの課題＋タイマー→写真提出→9軸採点・passGap・対話→自己ベスト演出→これまでで成長グラフ/進捗バー/バッジ/ストリーク
  - セキュリティ：ブラウザコンソールで `firebase.database().ref('dessin/hack').set('x')` → **PERMISSION_DENIED**
  - 誤PINで先生不可
- [ ] **Step 3: README**（任意）：`wake-path/README.md` に概要・技術・由来。
- [ ] **Step 4: 最終コミット** `git commit -m "chore: Wake Path production polish"`

---

## Self-Review（記入済み）

- 仕様カバレッジ：コアループ(Task5/6/7/9)、9軸採点(Task3/7)、固有色は texture 軸の定義に内包(ai.js AXES)、AI+先生協働の課題(Task5/ai assignmentChat)、制限時間必須(db saveStudent/assignment timeLimit＋Task6タイマー)、経過グラフ/進捗バー(Task8)、継続の仕掛け4種(Task6ストリーク+ボーナス、Task7自己ベスト+バッジ、Task8進捗バー+バッジ表示)、手描き添削(Task9)、セキュア(Task2/3 PIN・APIのみ書込)。
- プレースホルダなし：API は完全コード。クライアントは「要点を完全コード＋DOM/ロジック仕様」で、各画面の関数名・dbWrite/aiCall のアクションとペイロード形を明示。
- 型整合：9軸キー(proportion…process)を db.js/ai.js/データ契約で統一。dbWrite/aiCall アクション名は db.js/ai.js の switch と一致。
